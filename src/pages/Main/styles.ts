import styled from '@emotion/styled'
import bgImg from '@shared/assets/images/bg.png'

export const Container = styled.div`
  position: relative;
  width: 100%;
  height: 100%;
  background-image: url(${bgImg});
  background-size: cover;
  background-position: right bottom;
`
